# Ringtone-Test

**Installation instructions**

1. Have node.js installed: `brew install node`


2. Install hexo interpreter: `npm install hexo-cli -g`

3. `cd` into directory: `cd Ringtone-Test`

4. Run server: `hexo server`
